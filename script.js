const button=document.querySelector('#menuButton');const nav=document.querySelector('#mainNav');button?.addEventListener('click',()=>{const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const dialog=document.querySelector('#lightbox');const img=document.querySelector('#lightboxImage');document.querySelectorAll('.zoom').forEach(b=>b.addEventListener('click',()=>{const source=b.dataset.src||b.querySelector('img').src;img.src=source;img.alt=b.querySelector('img').alt;dialog.showModal()}));document.querySelector('#closeLightbox')?.addEventListener('click',()=>dialog.close());dialog?.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});